# HAUZ backend interview task

TanStack Start + TypeScript + Appwrite (Auth, Functions, TablesDB). Implements Personal Account registration with email verification, login, and logout against the provided HAUZ schema.

Full setup instructions (env vars, Appwrite project configuration, running the app, deploying the Function, assumptions & limitations) are written up in Phase 6 of this build and will replace this placeholder.
